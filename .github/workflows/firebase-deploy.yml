# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - better
env:
  PNPM_VERSION: '9.1.4'
  env_var: 'dev'

jobs:
  build_and_deploy:
    runs-on: ubuntu-22.04
    strategy:
      matrix:
        node-version: [22]
    steps:
      - uses: actions/checkout@v4
      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'pnpm'
      - name: Install dependencies
        env:
          API_KEY: "AIzaSyAL5cNWwfnYMnKVtIcDXL-ycHsjGvtnqRs"
          AUTH_DOMAIN: "testing-40917.firebaseapp.com"
          PROJECT_ID: "testing-40917"
          STORAGE_BUCKET: "testing-40917.appspot.com"
          APP_ID: "1:60050837084:web:9350a34b710c5ced67e099"
          MESSAGING_SENDER_ID: "60050837084"
          MEASUREMENT_ID: "G-MBHSN5X03W"
        run: pnpm add -g @quasar/cli && pnpm install && quasar build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_TESTING_40917 }}"
          channelId: live
          projectId: testing-40917
